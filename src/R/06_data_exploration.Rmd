---
title: "Data Exploration"
author: "Archie Benn"
date: "`r Sys.Date()`"
output: html_document
---
```{r setup, include=FALSE}
rm(list = ls())

knitr::opts_chunk$set(echo = TRUE)

library(tidyverse)
library(sf)
library(rnaturalearth)
library(countrycode)
library(ggrepel)
library(tikzDevice)
library(here)
library(skimr)
```

## Data Import
- Full data set from the end of `05_filtering.R`  
- This contains all 73 sites from FLUXNET2015 with ages from besnard2018 and LAI from MODIS attached  
- Not scaled yet (will happen in `06_engineer.R`, just before ML training etc. begins)

```{r}
df_main <- read_csv(here("data/fluxnet/05_filtering/filtered_main.csv"))
df_Rn <- read_csv(here("data/fluxnet/05_filtering/filtered_Rn.csv"))

# quick look at structures
skim(df_main)
```

- No missing data in main dataset

```{r}
skim(df_Rn)
```

- Also no missing data in Rn dataset




