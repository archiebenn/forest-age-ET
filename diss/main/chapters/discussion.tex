%----------------------------------------------------------------------------------------
%	DISCUSSION  1750 words
%----------------------------------------------------------------------------------------
\section{Discussion}
\subsection*{What this study addressed 100 words}
This study used ML models to form predictions of daily ET at FLUXNET forest sites across Europe and North America. While many previous studies have used FLUXNET with ML for ET predictions \parencite{}, using the age of a forest as a feature in these models has not been explored previously, and this study represents the first use of this feature for predictive ET modelling using ML methods. 

\subsection*{Key findings/results 250 words}
In all analyses contained within this study, site age contributed a meaningful impact on predictions of daily ET formed by RF models. The impact of using site age resulted in nuanced predictive accuracy changes, with large variation in the direction and magnitude of these performance shifts between sites in most analyses. 


\subsection*{Interpret results and compare with other papers/literature 600-700 words}
\subsubsection{Clustering}
\par Cluster analysis with PAM has been used successfully in hydro-ecology research previously \parencite{hou2025site, e2025multivariate}, but literature searches suggest that its use in ML ET predictive modelling is not common. Across cluster medoids in this study, model performance was highly diverse, with one suggestion for this being that training sites were pooled from a combination of sites across ecologically different clusters in all analyses. This may have led to under representation of specific cluster types in the training data and could explain why predictions at US-Me3, a geographically and characteristically distant site compared to others in the study (\autoref{fig:map} and \autoref{tab:cluster_characteristics}), were poor across all analyses - only 3 sites exist within the defined cluster, meaning that a maximum of 2 sites in the training pool could ever pass information into the RF models for predictions. \textcite{ferreira2022exploring} avoided this issue by training specific models which were trained and tested internally within each cluster. Clustering in \textcite{ferreira2022exploring} was also defined by variables which form the base of the FAO56-PM equation (used to calculate reference evapotranspiration $ET_0$ \parencite{allen2006recommendation}), ensuring that the resulting clusters are relevant to the ET estimations. While not formally verified, PAM clustering using Gower's distances in this study appeared to capture climate-relevant patterns, and could be of use for future research in ET generalisation modelling as a method to quantify regional diversity. 

\subsubsection{Model Sensitivity and Feature Selection}
Given the limitation of ML methods to extrapolate outside of feature values which have been `seen' during training \parencite{}, input feature selection has direct implications in model accessibility, interpretability, and predictive accuracy \parencite{liyew2025review}. Initial sensitivity testing at cluster medoids served two purposes. It provided direct evidence that feature selection can enhance and degrade model performance differently across sites, in agreement with the idea that no single, universally optimal feature set exists for ET predictions \parencite{liyew2025review}. This testing also established that the inclusion of forest age as a feature provided a measurable impact: the addition of site age was never neutral in terms of performance in any of the medoid test site predictions, providing initial evidence that site age is impacting the model's decision structure before more in depth analyses were undertaken. Models trained using easily acquired or measured variables should improve accessibility to ET predictions in data sparse regions, but predictive accuracy may be limited compared to models which are able to learn from a greater number of input sources \parencite{liyew2025review}. The most predictable medoid sites in this study - US-UMd, DE-Lnf, and CA-Obs - typically followed an upward trajectory before plateauing with an increasing number of input features, and typically a strong level of predictive accuracy could be reached using just 2-3 input features, in agreement with recent findings in \textcite{nayak2026estimation}.


% nayak2026estimationfound that using all available data yielded r-squared of 0.98, but with sparse data dropped to 0.78. also, said that using just 2/3 params gave promising results (Tair and Wspeed).

% liyew2025review really nice review paper on feature selection, including critical insights into data sources and selections of model features. could discuss how this relates to my selection of variables/the FLUXNET in general. Use especially in the section on forward feature selection (1.1). 

% liyew2025review also explicitly does not mention forest age when going ocver the various features used in ET preds - ie. highlight that is has not been used before really even when other biophysical features have been. also mentions that 'models can be trained for different plant functional types in section 3.1 - what about different species/swoodland?'

% liyew2025review - "Therefore, there should be a need for flexible and adaptive feature selection strategies rather than relying on a single, universally optimal feature set". also mentions the feature selection methods of filter, wrapper, and embedding. none of wehich i used but could have been applied - ccan critique mine there/indicate how this could lead to better performance in terms of accuracy and computational cost (my forward selection is actually a type of wrapper method)

% liyew2025review - Future research should continue exploring hybrid architectures alongside dynamic feature selection frameworks to enhance model adaptability and reliability. The integration of feature selection with advanced ML techniques will further refine predictive performance, enabling more precise and scalable AET estimation models.


\subsubsection{ET Generalisation}
\par Results from sampled training datasets in section \ref{sec:sampled} were also highly variable in overall performance and performance of models including site age. \textcite{ahmadi2025enhancing} formed models with the aim of enhance generalisation capacity and reduce overfitting predictions to a single site by training on diverse time series data from multiple sites, in a manner similar to the present study. However, model performance from \textcite{ahmadi2025enhancing} was far stronger and more stable across unseen sites compared to the present study. This is likely because, as in \textcite{ferreira2022exploring}, training and testing sites were confined to much smaller and similar geographic regions; all 55 sites from \textcite{ahmadi2025enhancing} were contained within the Central Valley of California - a climatically homogeneous region. In turn, training and test data were likely confined within a much narrower range compared to this study, potentially enabling higher predictive accuracy - although this is also confounded with the use of deep learning and automatic hyperparameter optimisation per model in \textcite{ahmadi2025enhancing}, which was not utilised here. This claim is partially supported by regression analysis in section \ref{sec:sampled}, where overall site similarity to the test site (using Gower's distance) was able to predict $R^2$ in both models across the full set of medoid sites, with accuracy falling as similarity reduced. This relationship was also significant at the finer site-level scale for 4/6 medoid test sites, indicating that the all site trend was not just an artifact from cross-site pooling.
\subsubsection{Forest Site Age as a Feature}
This study was building on work carried out in \textcite{besnard2018} in which forest age was used to 



% model sensitivity
% distances and mixed training datasets
% case study


\subsection*{Address limitations and potential impact on results 300 words }
% train/test disances
% unknown species
% age estimates
% fluxnet issues
% rf model/param choices fixed 
% clustering decisions


\subsection*{Implications and directions on/for future research 250 words}
% hybrid modelling - form target ET from defined ewuations
% species-specific modelling may help age estimates  








%%%%%%%%%%%%%%%%%%
% GENERAL NOTES/LITERATURE/IDEAS
%%%%%%%%%%%%%%%%%%

% jiang2023hybrid used a hybrid model. instead of using FLUXNET data as a target, they used a combined model of equations (budyko and something) and used the output of THIS as the target. not necessarily as true as FLUC tower observations, BUT means that a ML models which does well with predicting this equstion can essentially reduce the complexity of the equation itself to take more simple parameters! very cool idea and need to speak about this as a potential solution/looking forward with age included somehow...? - sidesteps the 'not enough aged forest flux site' issue i will discuss. &&& ahmadi2025enhancing also use penman menoteith as a target rather than requiring flux towers


% hill2017case concluded that at least four flux towers per site are required to confirm with a statistical confidence of 95 % that the flux over 1 year is not 0 and therefore to accept with a statistical confidence of 5 % that the annual flux is 0. This is of major importance for an ecosystem that is heterogeneous at a scale larger than the flux footprint of a single tower.


% stapleton2022framework found strong evidence that there is no singularly optimal machine learning algorithm or feature set across all of the wetland sites studied despite their similarities in wetland-based ML ET predictions. agrees with mine in some respects

% MAIN IDEA 1
% i think something key to mention overall about age is that while age itself may undermine several fcators (changes to water retention/evaporation/transpiration etc.), if the effects of age can themselves be decoded/explained by some other, more easily measured variable (perhaps one from a satellite like moisture retention index or something) then age itself becomes slightly redundant as a feature if/once those other variables are available at scale. ie. if what drives age-related changes in ET can be explained by a single other variable, measuring that variable itself would be enough to predict ET. however, if age-related changes are only encoded by other multiple, possibly interacting variables then age itself becomes a useful proxy for all of those and could be more easily measured perhaps. all comes down to whether age is an 'explainer', or an 'explained' variable (just come up with this so not sure if it works)

% nayak2026estimationfound that using all available data yielded r-squared of 0.98, but with sparse data dropped to 0.78. also, said that using just 2/3 params gave promising results (Tair and Wspeed).


%State it as a genuine open question. "Why forest age exerts opposite directional effects on ET predictions at these two sites remains unclear from this analysis" — then speculate briefly (a sentence or two, clearly flagged as speculation) about plausible mechanisms: species-specific hydraulic/canopy aging trajectories, non-linear age-ET relationships where different sites sit on different parts of the curve, or age interacting with something site-specific (canopy structure, climate) that isn't captured by your other features. This is the most defensible option — you're not overclaiming, and it's a legitimate direction for "future work."



%%%%%%%%%%%%%%%%%%
% ANALYSIS 1.1 - FEATURE SELECTION/MODEL SENSITIVITY
%%%%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%%%%
% ANALYSIS 1.2 - SHUFFLED
%%%%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%%%%
% ANALYSIS 1.3 - CASE STUDY=
%%%%%%%%%%%%%%%%%%

% in the US-UMd, Mod+Age improves over Mod. Looking at obs vs preds can see how some winter predictions are being overestimated by Mod, and this seems corrected by Mod+Age. Comparing this to SHAP it becomes clear that overall at this site the model with age is using site age negatively so it oculd be bringing down the predictions somewhat. Likewise, an opposite response is seen with DE-Lnf where SHAP shows site age is pushing predictions higher, particularly in the autumn and winter months and is hurting the predictions.

% however, looking at training dataset, the average site age is 111 years old. DE-Lnf average in the tests is 122, while US-UMd average is 94. The simplicity of this is that RF is likely using age as a site-level identifier (as site age differs minimally within sites, and maximally between sites unlike other variables which are more dynamic and direct drivers of ET). Essentially, there must be a relationship between age and ET, even if weak, in the training set (which RF learns) in which older sites have higher ET than younger sites. Therefore the RF uses site age to push ET predictions up if the site age > trining average, and down if site age < average (which is what heppens in this case). So age is being seen as a site level offset, rather than as a daily predictor.

% are these issues/benefits with site age coming during winter months when the model has less variable data in other predictors to rely on, but the site age is constant and so the additive/drop of each becomes more obvious?










